# godsh v0.2.5 升级实施进度

> 依据 `程序0.2.5升级优化.md` 分阶段实施。每完成一项在此勾选并记录验证结果。

## 阶段总览

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| M1 | 市场下载修复（D1–D8）+ 市场更新反馈（U1–U4） | ⬜ |
| M2 | 批量扩容（B1–B5）+ DSH 更新提示（U5–U7） | ⬜ |
| M3 | 性能优化（P1/P2/P4/P5） | ⬜ |
| 发布 | 0.2.5 打包 + 文档 | ⬜ |

---

## M1：市场下载修复 + 市场更新反馈

### 后端
- [x] D1: `run()` 支持超时（安装 180s）— installer.ts pluginAction 默认 timeoutMs
- [x] D2: 安装/更新/卸载日志落盘 `data/logs/plugin-*.log` — profiles.ts logPluginAction
- [x] D3: 安装接口返回结构化错误（errorType/message/logFile）— classifyPluginError
- [x] D4: 批量安装同样加超时 + 日志 + 错误归类
- [ ] U1: 新增插件更新后台任务接口（延后：先完成 D/B/U5-7）

### 前端
- [x] D5: 安装失败展示 errorType + 原因（errorLabel）+ 日志路径
- [ ] U2/U3/U4: 更新走后台任务 + 进度面板 + 成功/失败 Toast（延后）

### 验证记录
- typecheck / build:server / build:web 全过
- 【根因排查完结】“插件市场不能下载”真实原因 = 用户运行旧版 exe（CORS 白名单缺失 → Tauri 前端跨域请求被拦截）。新版已含 CORS 修复。
- 【验证】端到端实测（2026-08-28）：GET /api/market ✓、GET /api/profiles/web/plugins 返回 installedNames（含 @furongjun1999/dsh-memory、dsh-tools）✓、POST 安装返回 ok=true code=0 ✓、CORS 头 Access-Control-Allow-Origin: http://tauri.localhost ✓
- 【澄清】`dsh plugin add` 一直工作正常：写入真实 `~/.dsh/profiles/web/package.json` 的 dependencies + bundles（此前“package.json 保持 {}”是隔离测试假象——dsh shim 内部强制覆盖 DSH_HOME 到 `C:\Users\Shengmingkai\.dsh`）
- 【打包修正】0.2.5 release 旧包（23:31）不含 pkgName 修复（MarketPage 用 p.npm||p.name，23:46 才改）→ 已重新构建 server+web 并重新打包 0.2.5

---

## M2：批量扩容 + DSH 更新提示

- [x] B1: 市场列表分批渲染（去 slice(0,200)，初始 60 + 加载更多）
- [x] B4: 可选/已选计数显示（toolbar + 列表头）
- [x] B5: 批量安装无上限（串行队列已有）
- [x] U5/U6/U7: DSH base 更新完成/失败 Toast + 任务详情（prevTasks 变化检测）

### 验证记录
- typecheck / build:web 全过
- 待：运行验证批量 10+、更新结束 Toast

---

## M3：性能优化

- [x] P1: scanProfiles 内存缓存（1s TTL + 目录 mtime 签名）+ 主动失效（allocation/unified-kernel/profile-editor 写回点）
- [x] P2: 市场索引本地持久化缓存（data/cache/market.json，7 天 TTL + 损坏回退网络）
- [x] P4: 日志轮询 2s→3s、全局搜索防抖 300ms、Dashboard 后台页暂停轮询
- [x] P5: findDshInstances 版本探测缓存 10s

### 验证记录
- typecheck / build:server / build:web 全过
- 单测 20/20、冒烟 6 组全绿（scan 缓存未破坏分配/统一内核）
- 待：运行验证市场秒开（本地缓存）、轮询请求数下降

---

## 发布

- [x] bump-version 0.2.5（已统一）
- [x] typecheck + test + test:smoke 全过
- [x] make-release 0.2.5 + 桌面验证（0.2.5 运行正常）
- [x] 更新 RELEASE_NOTES / CHANGELOG
- [x] 【重打】0.2.5 重新打包（补入 pkgName 修复：市场 name≠npm 时用 npm 字段匹配已安装状态）
- [x] 【品牌改名】项目改名 **godsh**（73+ 文件：package.json/@godsh/* 包名、Cargo/tauri.conf/productName=godsh、窗口标题、README/文档、GitHub 指南 shengmk/godsh）
- [x] 【图标替换】两张新图：图1 → tauri icon 全套（icon.ico/png/icns/Square*/android/ios）；图2 → 侧边栏品牌 logo（/logo.png，CDP 实测 loaded）+ assets/godsh-banner + social-preview
- [x] 【重打包】godsh-0.2.5-x64-setup.exe + zip（SHA256 通过），CDP 验证：标题 godsh、logo 加载、7 导航
- [x] git 提交（改名 godsh）
- [x] git 推送到 GitHub（shengmk/godsh，分支 main；代理 127.0.0.1:12450；分支已从 master 改名为 main）

---
