# 项目文件总结（godsh）

> 面向开发者 / 下一个迭代会话的项目全貌说明。版本 v0.1.0（git: 4 commits）。

## 1. 项目是什么

**godsh** —— DeepSeek Harness（dsh）的图形化环境配置启动器，类比 Anaconda Navigator：

| Anaconda | DSH | godsh |
| --- | --- | --- |
| Anaconda Navigator | dsh web / CLI | 本启动器 |
| conda env | DSH Profile | 环境管理 |
| pip/conda 包 | dsh plugin / bundle | 插件分配与安装 |
| conda channel | dshmarket | 插件市场 |
| Python | DeepSeek Harness | dsh 本体（底层调用） |

交付形态：**TypeScript 后端（CLI + HTTP API）+ Web 管理界面（Vite+React）+ Tauri 2 桌面壳**。

## 2. 总体架构

```text
┌────────────────────────────────────────────────┐
│  UI 层                                        │
│  ├─ apps/shell-web   Web 界面（Vite+React+TS） │
│  └─ apps/launcher/src-tauri  桌面壳（Tauri 2） │
└──────────────┬─────────────────────────────────┘
               │ HTTP (127.0.0.1:4780)
┌──────────────▼─────────────────────────────────┐
│  Node API 后端（apps/launcher/src/server.ts）  │
│  环境 / 插件 / 市场 / 分配 / 内核              │
└──────────────┬─────────────────────────────────┘
               │ 调用 dsh CLI / 读写配置
┌──────────────▼─────────────────────────────────┐
│  DSH Profile（$DSH_HOME/profiles）            │
│  package.json · cordis.patch.yml              │
└────────────────────────────────────────────────┘
```

关键设计：
- **同一个 Web 内核**：所有 client 插件统一由 dsh 的 `dsh-client-web` 内核加载（DSH 原生机制）。
- **桌面应用自包含**：后端 `server.mjs` + 内核模板打进资源；运行时数据存 `%APPDATA%\godsh\data`。
- **发布打包**：`tauri build`（内嵌前端）→ 自定义 NSIS 安装器 + 便携 ZIP（`WebView2Loader.dll` 放 exe 旁）。

## 3. 目录结构与文件职责

### 根目录

| 文件 | 作用 |
| --- | --- |
| `package.json` | 工作区根：脚本（dev/launcher/serve/typecheck/build:web/build:server）与工具依赖 |
| `pnpm-workspace.yaml` | pnpm workspace（apps/* 与 packages/*） |
| `tsconfig.base.json` / `tsconfig.json` | TypeScript 基础配置（后端范围，排除 shell-web） |
| `.gitignore` | 忽略 node_modules/dist/target/release 二进制/运行时数据 |
| `README.md` | 项目总览与运行说明 |
| `QUICKSTART.md` | 快速开始（安装/首次启动/三步上手） |
| `使用说明.md` | 完整使用手册（功能/CLI/API/配置/桌面构建/故障排查/开发） |
| `CHANGELOG.md` | 变更日志 |
| `LICENSE` | MIT 许可证 |
| `PROMPT.md` | 常驻 AI 开发提示词 |

### apps/launcher —— Node 后端 + CLI + Tauri 壳

| 文件 | 作用 |
| --- | --- |
| `src/cli.ts` | CLI 入口（detect/profiles/plugin/start/stop/kernels/allocate/apply/serve 等命令） |
| `src/context.ts` | 共享上下文：ConfigStore、EventBus、各 Manager（含 DSH 根目录 `~/.dsh` 兜底） |
| `src/server.ts` | HTTP API 服务（node:http）：`/api/health|profiles|plugins|market|allocations|kernels` + 静态回退 |
| `src-tauri/Cargo.toml` | Tauri Rust 依赖（tauri 2.11，`crate-type=["rlib"]`——GNU 下 cdylib 会导出表溢出） |
| `src-tauri/tauri.conf.json` | 窗口/标识/资源（`resources` 目录）/frontendDist |
| `src-tauri/src/main.rs` | 入口（release 无控制台） |
| `src-tauri/src/lib.rs` | 启动时从资源解析 server.mjs/templates 并 spawn 后端；`RunEvent::Exit` 杀后端 |
| `src-tauri/resources/` | 运行时资源（server.mjs + templates，构建时生成） |
| `src-tauri/icons/` | 应用图标（Tauri 默认） |

### apps/shell-web —— 前端（Vite + React 18 + TS）

| 文件 | 作用 |
| --- | --- |
| `src/App.tsx` | 侧边栏导航 + 页面切换（含 Ctrl+1..4 快捷键） |
| `src/api.ts` | REST 封装（`VITE_API_BASE` 可覆盖，Tauri 模式指向 4780） |
| `src/types.ts` | 前端类型定义 |
| `src/hooks.ts` | useToast 钩子 |
| `src/components.tsx` | Toast / Loading / ContextMenu 通用组件 |
| `src/pages/ProfilesPage.tsx` | 环境页：启动/停止/日志（2s 轮询）/右键菜单/3s 状态刷新 |
| `src/pages/MarketPage.tsx` | 插件市场：搜索/安装/中文描述（zh 优先）/分类/星标/下载量 |
| `src/pages/AllocationsPage.tsx` | 插件分配：新增/启停/移除/拖拽排序/右键菜单/上移下移/应用写回 |
| `src/pages/KernelsPage.tsx` | 内核管理：模板 + 实例（新建/启停/删除） |
| `src/styles.css` | 玻璃拟态设计系统（蓝色渐变、半透明白卡、大圆角） |
| `vite.config.ts` | Vite 配置（dev 代理 /api → 4780） |
| `.env.tauri` | Tauri 构建环境变量（`VITE_API_BASE=http://127.0.0.1:4780/api`） |
| `tsconfig.json` / `vite-env.d.ts` | TS 配置与 Vite 客户端类型 |

### packages/ —— 后端领域包

| 包 | 文件 | 职责 |
| --- | --- | --- |
| `core` | `env-detect.ts` | 检测 node/pnpm/dsh/DSH_HOME |
| | `run.ts` | 命令执行（Windows cmd shim 兼容 + 参数转义）、findInPath、killProcess |
| | `process-manager.ts` | dsh web 启停、端口就绪探测、pid 文件、日志截断 |
| | `config-store.ts` | JSON 数据文件读写（data/） |
| | `events.ts` | 类型安全事件总线 |
| | `paths.ts` | 根目录/数据/模板/插件路径（支持 `DSH_LAUNCHER_*` 环境变量覆盖） |
| | `types.ts` | EnvInfo / LauncherConfig |
| `profile-manager` | `scanner.ts` | 扫描 `$DSH_HOME/profiles` 的 bundles/依赖/patch |
| | `profile-editor.ts` | 创建/删除 Profile、更新 bundles |
| | `patch.ts` | cordis.patch.yml 轻量解析/序列化 |
| `plugin-registry` | `bundle.ts` | bundle/client 插件识别 |
| `marketplace` | `dshmarket-client.ts` | 拉取 awesome-dsh-plugin 索引 |
| | `installer.ts` | 封装 `dsh plugin add/remove/update/ls` |
| `kernel-manager` | `kernel-template.ts` | 内核模板读取 |
| | `kernel-process.ts` | 内核实例 CRUD + 启停（持久化 data/kernels.json） |
| `allocation` | `allocation-manager.ts` | 插件分配 CRUD + 顺序 + 写回 Profile patch |
| `security` | `source-policy.ts` | 插件来源白名单（registry/local 允许，git 需授权） |
| | `build-permission.ts` | pnpm allowBuilds 管理 |

### 数据与配置

| 文件 | 作用 |
| --- | --- |
| `data/config.json` | Launcher 全局配置（dsh.home、默认端口、市场 URL） |
| `data/plugins.json` / `kernels.json` / `allocations.json` | 运行时数据（桌面版在 `%APPDATA%\godsh\data`） |
| `kernels/templates/web-default/template.json` | 内核模板 |
| `plugins/hello-world/` | 本地 bundle 插件示例 |
| `profiles/web/` | 本地 Profile 模板示例 |
| `release/` | 发布产物：setup.exe + zip + SHA256SUMS + RELEASE_NOTES |

### docs/ —— 设计文档

| 文件 | 作用 |
| --- | --- |
| `dsh-study.md` | DSH 框架学习笔记（含 `dsh web` 别名等真实机制） |
| `workflow.md` | 业务流程 |
| `folder-config.md` | 目录与配置约定 |
| `gui-design.md` | GUI 风格设计（玻璃拟态） |
| `iteration-log.md` | 迭代日志 |

### scripts/ —— 构建脚本

| 文件 | 作用 |
| --- | --- |
| `dev.ps1` | 开发（pnpm install + launcher） |
| `build.ps1` | 类型检查 |
| `build-tauri.ps1` | 构建 Tauri debug 壳（无空格路径 + MinGW） |
| `make-release.ps1` | **发布打包**：build:server → build:tauri → tauri build（内嵌前端）→ ZIP + NSIS → 校验和 |
| `bump-version.ps1` | 统一升级版本号 |
| `pack-plugin.ps1` | 本地插件打包 |

## 4. 构建 / 运行

```powershell
pnpm install                       # 安装依赖
pnpm typecheck                     # 后端类型检查
pnpm build:web                     # 前端（Web 模式）
pnpm serve                         # Web 模式：http://127.0.0.1:4780
pwsh -File scripts/make-release.ps1  # 发布打包（安装器 + ZIP）
```

桌面发布注意：必须用 `tauri build`（内嵌前端）；`WebView2Loader.dll` 放 exe 旁（GNU 工具链必需）。

## 5. 当前状态

- ✅ 后端全链路（环境/插件/市场/分配/内核）+ CLI + HTTP API
- ✅ Web 界面四页 + 玻璃拟态 + 交互（启停/日志/拖拽/右键/快捷键）
- ✅ Tauri 桌面壳（自包含，安装器 + ZIP 可分发）
- ✅ 三个已知问题已修：`dsh web` 命令、WebView2Loader.dll、界面 404；环境兜底 `~/.dsh`；市场中文化

## 6. 已知限制 / 待改进（详见《优化方案大纲.md》）

- 无 Web 内核的 Profile 无法直接跑 Web UI（统一内核方案未实现）
- 插件分配靠输入 ID，而非可视化拖拽树
- 无 dsh 版本管理（只能在 `~/.dsh` 用当前 dsh）
- 无设置页（语言/路径/主题）
- 侧边栏数字快捷键与页面功能说明待改进
- UI 可进一步美化
