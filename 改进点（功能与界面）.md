# 改进点清单（功能与界面）

> 汇总用户反馈 + 我的建议。完整方案见《优化方案大纲.md》；本文件是可勾选的改进清单。

## A. 功能改进

### A1. 统一内核（用户重点诉求）
- [ ] 每个 Profile（即使 bundles 无 `dsh-web-app`）都能启动 Web UI
- [ ] 统一内核可配置：用户可添加/移除自己偏好的插件
- [ ] 统一内核通过 `--patch` 注入，不污染用户 Profile 配置
- [ ] 内核管理页支持编辑「统一内核」插件列表与顺序

### A2. 插件分配可视化（用户重点诉求）
- [ ] Profile 列表可展开成树
- [ ] 插件直接拖拽排序 / 拖到其它 Profile
- [ ] 从「已安装」点选添加（不输入 ID）
- [ ] 右键菜单：上移/下移/移除/查看
- [ ] 已安装但未分配的插件自动提示

### A3. dsh 版本管理
- [ ] 检测多个 dsh 版本（PATH / npm 全局 / 自定义目录）
- [ ] 设置页可设默认版本；Profile/内核可指定版本
- [ ] 启动命令使用指定版本绝对路径

### A4. 设置页（新增）
- [ ] 语言：zh-CN / en（i18n）
- [ ] 主题：浅色 / 深色 / 跟随系统
- [ ] 文件位置：DSH 根目录、数据目录、日志目录、模板目录
- [ ] 市场 URL / 启用
- [ ] pnpm allowBuilds 白名单

### A5. 其它功能
- [ ] 环境启动阶段进度（spawn → 就绪 → 完成）
- [ ] 内核实例日志查看
- [ ] 插件安装进度提示
- [ ] 批量启停环境
- [ ] 数据导出/导入备份
- [ ] 端口占用/冲突提示
- [ ] Tauri 托盘 + 系统通知
- [ ] 启动失败时展示 dsh.log 尾部 + 修复建议

## B. 界面改进

### B1. 侧边栏（用户明确要求）
- [ ] 去掉数字 `1234` 角标
- [ ] 同一位置显示该页面功能说明
- [ ] 保留（可选）`Ctrl+1..4` 快捷键但不显示数字

### B2. 一体化与美化
- [ ] 首页仪表盘（环境/插件/内核统计卡片 + 快捷操作）
- [ ] 统一组件库：Card / Badge / Button / Modal / Tooltip / Skeleton / Empty / Switch / Tabs
- [ ] 卡片 hover 动效、入场动画
- [ ] 骨架屏加载
- [ ] 空状态插画
- [ ] 图标库（如 lucide-react）替换 emoji/文字
- [ ] 深色主题完整适配
- [ ] 响应式布局（窄窗口自适应）
- [ ] 键盘无障碍（焦点/Tab）

## C. 技术债 / 工程

- [ ] `paths.ts` 模块级常量改为可配置（支持设置页改路径后生效）
- [ ] 前端加入 i18n 框架或轻量语言包
- [ ] 统一 API 错误处理与前端错误提示
- [ ] 增加基础测试（后端单元测试：patch 解析、allocation、kernel）
- [ ] 发布流程文档化（GitHub Actions 自动打包）
- [ ] 图标替换为项目自有图标

---

**优先级建议**：A2（插件分配可视化）→ A1（统一内核）→ A4 设置页（主题/语言）→ A3（dsh 版本）→ B1（侧边栏）→ B2（美化）。
