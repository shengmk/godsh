# godsh 沙箱自动更新失效修复与工作栏任务通知全闭环方案

> 方案详情已在 [docs/方案/方案.md](docs/方案/方案.md) 全量归档并持续更新。

## 核心修复概览

1. **根治沙箱自动更新失效 (P0)**：
   - **解压路径修复**：在 `packages/plugin-registry/src/vault.ts` 中修正 `tar` 解压路径为完整绝对路径，显式设置 `{ cwd: tmpPackDir }`，根治 Windows `tar.exe` 找不到压缩包报错；
   - **异步并发网络比对**：将 `checkUpdates()` 串行同步 `spawnSync('npm', ...)` 彻底重构为原生异步并发 `fetch` 探测，杜绝 90 秒主线程卡死与 HTTP 超时；
   - **流式日志注入**：为 `updatePlugin` 与 `updateAll` 注入实时回调。

2. **打通工作栏（全局任务中心）全闭环 (P0)**：
   - **后端异步任务派发**：在 `apps/launcher/src/routes/vault.ts` 中为 `/api/vault/update-all` 与 `/api/vault/update` 接入 `ctx.startInstallTask`，新增 `GET /api/vault/task-progress` 实时进度接口；
   - **前端任务中心全面纳管**：在 `apps/shell-web/src/tasks.ts` 中新增 `vault-update-all` 与 `vault-update` 任务类型与 `taskManager.startVaultUpdateAllTask()`，实现 1 秒高频轮询、进度条步进与流式日志回显；
   - **右下角工作栏交互**：联动 `TaskCenter.tsx`（旋转微标、状态徽章、展开日志抽屉）；
   - **双页面接入**：重构 `VaultHubPage.tsx` 与 `AllocationsPage.tsx`，一键更新直接派发至全局工作栏，支持离屏追踪与系统原生桌面通知。
