# godsh v0.2.5 升级优化方案（详细版）

> 本文档是 v0.2.4 → v0.2.5 的**完整升级优化方案**，供 AI 编程会话与用户逐项实施、勾选验收。
> 背景：用户反馈四类问题——① 插件市场不能下载；② 批量选择数量少（期望 ≥10）；③ 更新无进度/无成功失败提示；④ 程序速度慢。
> 原则：先修正确性（下载/反馈），再提升体验（批量/更新），最后做性能；每步跑 `pnpm typecheck` + `pnpm test` + `pnpm test:smoke`。

---

## 0. 调研结论（先讲事实）

### 0.1 市场「不能下载」的现状
- 链路：前端 `install()` → `POST /api/profiles/:name/plugins {action:'add',pkg}` → `pluginAction` → `run('dsh',['plugin','--profile',profile,'add',pkg])`
- 代码缺陷：
  1. **`run()` 默认无超时**（`run.ts` 第 64 行：`opts.timeoutMs ? ... : null`）→ `dsh plugin add` 底层 pnpm 可能**无限挂起**，前端一直转圈无反馈；
  2. 失败时前端只显示 `r.stderr`，但 dsh/pnpm 的 stderr 可能为空 → 显示「安装失败」无原因（用户看到的"未知原因"）；
  3. **无安装日志落盘**，出问题无法事后查；
  4. `sourcePolicy` 拒绝来源（git/local）时只回 `403 + reason`，前端没有把 reason 突出展示；
  5. 单装与批量安装是**两套代码**（`install()` 与 `batchInstall()` 都各自调 `installPlugin`），错误处理不一致。

### 0.2 「只能选 5 个」的真相
- 市场索引 **2467 个插件**；前端 `sortedPlugins.slice(0, 200)` **只渲染前 200 个**（MarketPage.tsx 第 278 行）。
- `selected` 是 `Set`，**本身无数量上限**；用户"最多选 5 个"的实际原因是：**默认排序下前 200 个里未安装、可选中的插件恰好只有少数几个**（大量已安装被 `disabled`，或热门区就几个新的）。
- 结论：不是 Set 限制，而是**列表截断 + 无分页/加载更多 + 已装插件占位** 造成的"可选数量少"。

### 0.3 更新无反馈的现状
- **市场插件更新**：`update()` 同步 await，只有按钮文字「处理中…」，无进度/阶段；成功失败有 toast，但 stderr 空时无原因。
- **DSH base 更新**：`POST /api/dsh/update` 启动后台任务（installTasks + 日志文件），前端 `load()` 每 2s 轮询 `tasks`，**有进度日志面板**（runningTask 显示），但**任务结束（done/error）没有任何 Toast**，用户不知道完成与否。
- 缺统一：市场插件更新没走后台任务机制（有进度可显示），DSH 更新走了但没结束提示。

### 0.4 性能瓶颈（实测定位）
| 热点 | 位置 | 成本 |
| --- | --- | --- |
| `scanProfiles` 全量磁盘扫描 | 17 处调用（profiles 列表、available、统一内核、reset、CLI…） | 每 3s 轮询反复读所有 Profile 的 package.json + patch |
| `allocations.list()` 每次读文件 | allocation-manager（无内存缓存） | 每次 API 调用都读磁盘 |
| 市场索引每次拉全量 | `fetchMarketIndex`（仅 5min 内存缓存，无本地持久化） | 重启后重新下载 2467 条（~几百 KB） |
| 市场 200 张卡片一次性渲染 | MarketPage `slice(0,200)` + `.map` | DOM 重，滚动卡顿 |
| 多页各自轮询 | ProfilesPage 3s / KernelsPage 2s / DshEnvs 2s / Console 2s / Dashboard 5s | 每页独立请求 |
| 全局搜索每次输入触发 3 个请求 | App.tsx `runSearch`（无防抖） | 输入即打 profiles+plugins+kernels |
| `findDshInstances` 每次探测版本 | env-detect（5s 缓存，但 list() 每次调用） | 每环境跑 node 子进程 |

---

## 一、插件市场「不能下载」修复（正确性，P0）

### 1.1 后端：安装加超时 + 日志落盘 + 结构化错误

| # | 改动点 | 文件 | 说明 |
| --- | --- | --- | --- |
| D1 | `run()` 支持默认超时（安装类 180s） | `packages/core/src/run.ts` | 超时 `kill` 并返回 `{ok:false, error:'超时'}`；保留自定义覆盖 |
| D2 | 安装/更新/卸载前**写日志文件** `data/logs/plugin-<profile>-<pkg>-<ts>.log` | `apps/launcher/src/routes/profiles.ts` | 用 `createWriteStream` 追加 stdout/stderr，失败时可查 |
| D3 | 安装接口返回结构化错误：`{ ok, code, stdout, stderr, errorType, message }` | 同上 | 解析 dsh 输出归类：`network`（ECONNREFUSED/超时）/`not-found`（未找到包）/`policy`（来源拒绝）/`deps`（依赖冲突）/`other` |
| D4 | 批量安装 `POST .../plugins/batch` 同样加超时 + 逐包日志 + 错误归类 | 同上 | 与单装一致 |
| D5 | 前端 `install()`/`update()`/`remove()` 显示 `errorType + message`（含日志路径） | `MarketPage.tsx` | 不再「未知错误」；失败时提供「查看日志」入口 |

### 1.2 前端：安装反馈增强

| # | 改动点 | 说明 |
| --- | --- | --- |
| D6 | 安装中按钮显示阶段：`检测来源 → 执行 dsh → 校验` | 复用后台任务模式（见 U1）或至少显示「安装中…」+ 已耗时 |
| D7 | 失败卡片展示：错误类型徽标（网络/包不存在/来源拒绝/依赖冲突）+ 摘要 + 「查看日志」 | 可操作 |
| D8 | 「重试」按钮（失败后一键重发同一包） | 网络抖动友好 |

### 1.3 验收
- [ ] 安装超时（断网模拟）→ 明确提示「网络超时」而非无限转圈
- [ ] 包名不存在 → 提示「未找到包 @xxx」+ 日志
- [ ] 安装成功 → 列表出现「已安装」徽标 + Toast
- [ ] 批量 10 个（含 1 个失败）→ 队列标出失败包 + 原因 + 可重试

---

## 二、批量选择扩容（≥10，P0）

### 2.1 根因修复：列表截断 → 分页/加载更多

| # | 改动点 | 文件 | 说明 |
| --- | --- | --- | --- |
| B1 | 去掉 `slice(0,200)`，改为**分批渲染**：初始 60，滚动/「加载更多」每次 +60 | `MarketPage.tsx` | 2467 个全量可浏览 |
| B2 | 「全选当前已加载」按钮 + 「全选全部结果」按钮 | 同上 | 需确认：全选全部可能几百个，提示数量确认 |
| B3 | 已安装的插件默认**折叠到「已安装」分组**（或标记但不占可选位） | 同上 | 可选区聚焦未安装项 |
| B4 | 工具栏显示「可选 N · 已选 M」 | 同上 | 明确可选空间 |
| B5 | `selected` 保持 Set 无上限；批量安装支持任意数量串行队列（现有循环已支持） | 同上 | 10+ 无压力 |

### 2.2 验收
- [ ] 未安装插件 ≥10 个时能勾选 10 个并批量安装
- [ ] 列表可滚动加载到全部 2467 个
- [ ] 已安装插件不占可选位，可选数清晰

---

## 三、更新反馈完善（进度 / 成功 / 失败，P0）

### 3.1 市场插件更新 → 后台任务 + 进度日志

| # | 改动点 | 文件 | 说明 |
| --- | --- | --- | --- |
| U1 | 新增 `POST /api/profiles/:name/plugins/:pkg/update-task`（后台任务，复用 `startInstallTask`） | `routes/profiles.ts` / `server.ts` | 与 DSH 安装一致：任务 + 日志文件 + 前端轮询 |
| U2 | 前端更新按钮 → 启动任务 → 显示进度面板（阶段 + 日志尾部） | `MarketPage.tsx` | 复用批量队列面板样式 |
| U3 | 任务结束：**成功 Toast**「已更新 x → v1.2.3」；**失败 Toast**「更新失败：<原因>」+ 日志入口 | 同上 | 明确结束状态 |
| U4 | 更新后自动刷新 `installedNames` 与版本显示 | 同上 | |

### 3.2 DSH base 更新 → 结束提示

| # | 改动点 | 文件 | 说明 |
| --- | --- | --- | --- |
| U5 | DshEnvsPage 轮询到任务 `done` → Toast「base 已更新到 vX」；`error` → Toast「更新失败：<message>」 | `DshEnvsPage.tsx` | 目前只显示日志无结束提示 |
| U6 | 任务列表显示「完成时间 / 失败原因」 | 同上 | |
| U7 | 并列环境安装/删除同样补结束提示 | 同上 | 一致性 |

### 3.3 验收
- [ ] 市场插件更新：进度可见 → 结束有成功/失败 Toast
- [ ] DSH base 更新：完成/失败都有 Toast + 日志
- [ ] 断网更新 → 明确失败原因

---

## 四、性能优化（P1，按收益排序）

### 4.1 P1：`scanProfiles` 内存缓存 + 主动失效

| # | 改动点 | 文件 | 说明 |
| --- | --- | --- | --- |
| P1a | 新增 `profileCache`：`Map<dir, {info, mtime}>` + TTL 1s | `packages/profile-manager/src/scanner.ts` | 读文件前查 mtime，未变则用缓存 |
| P1b | 导出 `invalidateProfileCache(dir?)` | 同上 | |
| P1c | 写回点主动失效：分配 `applyProfile`、统一内核 `applyToProfile/revertProfile`、`setProfileBundles`、删除 Profile | allocation / kernel-manager / profile-editor | 保证数据新鲜 |
| P1d | 扫描失败不缓存（下次重试） | 同上 | |

### 4.2 P2：市场索引本地持久化缓存

| # | 改动点 | 文件 | 说明 |
| --- | --- | --- | --- |
| P2a | 首次拉取后写 `data/cache/market.json`（含 fetchedAt） | `packages/marketplace/src/dshmarket-client.ts` | |
| P2b | 读取：TTL 7 天内有本地文件直接用；超时/失败回退本地 | server.ts `getMarket()` | 重启免下载 |
| P2c | 损坏 JSON 自动回退网络 + 重建缓存 | 同上 | |

### 4.3 P3：市场列表分批渲染（已并入 B1）

### 4.4 P4：轮询去重与降频

| # | 改动点 | 说明 |
| --- | --- | --- |
| P4a | ProfilesPage 3s 轮询已合并接口（保留）；**日志轮询 2s → 3s** | 降频 |
| P4b | KernelsPage 日志 2s → 3s；DshEnvsPage 任务轮询仅在 running 时（已做） | 降频 |
| P4c | **App 全局搜索加防抖**（300ms） | `App.tsx` runSearch 用 setTimeout |
| P4d | Dashboard 5s 轮询仅页面可见时执行（`document.visibilityState`） | 后台标签页不轮询 |

### 4.5 P5：`findDshInstances` 探测缓存

| # | 改动点 | 说明 |
| --- | --- | --- |
| P5a | 版本探测结果缓存 10s（跨 `list()`/`settings` 复用） | env-detect |

### 4.6 可选 P6：Profile 详情懒加载（改动较大，可延后）

| # | 改动点 | 说明 |
| --- | --- | --- |
| P6a | `GET /api/profiles/names`（只读目录名）；详情按需 | 冷启动更快 |
| P6b | 环境页卡片先显示名称+状态，展开再拉详情 | 与现有合并轮询配合 |

### 4.7 验收（性能）
- [ ] 环境页 3s 轮询时磁盘扫描次数显著下降（日志/计数器验证）
- [ ] 重启应用后市场秒开（本地缓存）
- [ ] 市场滚动不卡（分批渲染）
- [ ] 后台标签页不轮询（资源占用下降）

---

## 五、实施顺序与里程碑

| 阶段 | 内容 | 验证 |
| --- | --- | --- |
| **M1（正确性）** | D1–D8（下载修复）+ U1–U4（市场更新反馈） | typecheck + 单测 + 冒烟 + 桌面运行 |
| **M2（体验）** | B1–B5（批量扩容）+ U5–U7（DSH 更新提示） | 同上 + 10 个批量实测 |
| **M3（性能）** | P1（scan 缓存）→ P2（市场缓存）→ P4（轮询/防抖）→ P5 | 计数验证 + 手动体验 |
| **M4（可选）** | P6 懒加载 | 如时间允许 |
| **发布** | `bump-version.ps1 0.2.5` → `make-release.ps1 0.2.5` → 更新 RELEASE_NOTES/CHANGELOG/README → git 提交 | 桌面端验收 |

---

## 六、风险与注意

| 风险 | 对策 |
| --- | --- |
| scanProfiles 缓存导致状态不刷新 | 写回点全部加 `invalidateProfileCache`；冒烟分配用例回归 |
| 市场本地缓存陈旧 | TTL 7 天 + 手动「刷新市场」按钮强制重拉 |
| 批量全选几百个安装过久 | 串行队列已有进度；提示「将安装 N 个，预计 X 分钟」 |
| 超时 kill 误杀慢速 pnpm | 180s 阈值可配置；日志记录 |
| 更新任务并发 | 任务 key 唯一（pkg），冲突返回 409（复用 DSH 安装模式） |

---

## 七、涉及文件清单（供实施对照）

| 层 | 文件 |
| --- | --- |
| 后端 core | `packages/core/src/run.ts`（超时）、`env-detect.ts`（探测缓存） |
| 后端 profile-manager | `src/scanner.ts`（缓存 + invalidate）、`src/profile-editor.ts`（失效调用） |
| 后端 allocation | `src/allocation-manager.ts`（applyProfile 失效调用） |
| 后端 kernel-manager | `src/unified-kernel.ts`（apply/revert 失效调用） |
| 后端 marketplace | `src/dshmarket-client.ts`（本地缓存）、`src/installer.ts`（超时参数） |
| 后端 launcher | `src/routes/profiles.ts`（安装日志/错误分类/update-task）、`src/server.ts`（market 缓存、startInstallTask 复用） |
| 前端 | `pages/MarketPage.tsx`（分页/批量/更新反馈）、`pages/DshEnvsPage.tsx`（结束提示）、`pages/ProfilesPage.tsx`/`KernelsPage.tsx`（轮询降频）、`App.tsx`（搜索防抖）、`api.ts`/`types.ts`（新接口与类型） |
| 文档 | `docs/iteration-log.md`、`README.md`、`release/RELEASE_NOTES.md`、`CHANGELOG.md` |

---

## 八、给下一个 AI 会话的执行要求

1. 先读本文件 + `PROMPT.md` + `docs/iteration-log.md`。
2. 按 M1 → M2 → M3 → M4 顺序实施，每阶段跑 `pnpm typecheck` + `pnpm test` + `pnpm test:smoke`。
3. 每阶段更新 `docs/iteration-log.md`；发布前更新 RELEASE_NOTES/CHANGELOG/README。
4. 冒烟输出重定向到 `$env:TEMP`（避免 `smoke-*.txt` 进 git）。
5. 禁止删除工作区根历史 md 文档。
