# godsh v0.2.2 验收清单

> 验收对象：`release/godsh-0.2.2-x64-setup.exe`（安装版）与 `release/godsh-0.2.2-x64.zip`（便携版）
> 本轮范围：**拖拽全面化 + 市场批量安装/排序 + 性能优化 + 删除增强（v0.2.2）+ A/B/C 三组工程改进**
> 前置：本机已安装 dsh CLI（建议 0.1.1-rc.1 及以上）与 Node.js ≥ 20；首次运行数据落在 `%APPDATA%\godsh\data`。

---

## 一、安装与启动

| # | 验收项 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| 1.1 | 安装器 | 双击 setup.exe 安装成功，开始菜单出现「godsh」快捷方式 | ☐ |
| 1.2 | 便携版 | 解压 ZIP 后直接运行 `godsh.exe`（exe 旁有 WebView2Loader.dll + resources/） | ☐ |
| 1.3 | 窗口 | 打开窗口标题「godsh」，界面正常渲染（玻璃拟态，左侧 7 页导航） | ☐ |
| 1.4 | 版本 | 顶栏显示 `DSH x.x.x · env/PATH`；`/api/health` 的 launcher.version = **0.2.2** | ☐ |
| 1.5 | 数据目录 | 首次运行后 `%APPDATA%\godsh\data\` 生成 config.json 等 | ☐ |

## 二、环境（Profile）管理

| # | 验收项 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| 2.1 | 环境列表 | 显示全部 Profile（bundle/依赖数量、状态徽标） | ☐ |
| 2.2 | 启动 | 点击「启动」→ 状态变「启动中…」→「运行中」，出现端口与「打开 ↗」 | ☐ |
| 2.3 | 统一内核 | **无 web-app 的 Profile（如 open-design/experiment）也能启动出 Web UI**（bundles 自动注入） | ☐ |
| 2.4 | 停止 | 停止后端口释放、状态回「已停止」 | ☐ |
| 2.5 | 日志 | 「日志」面板每 2s 滚动刷新，可暂停/继续 | ☐ |
| 2.6 | 新建环境 | 输入名称 → 新建（官方默认模板 base + web-app） | ☐ |
| 2.7 | 删除增强 | 卡片 🗑️ 按钮：运行中**禁用+Tooltip**；删除需**输入环境名**确认 | ☐ |
| 2.8 | 批量操作 | 勾选多个 → 「▶ 批量启动 / ⏹ 批量停止 / 🗑️ 批量删除（需输入 DELETE）」 | ☐ |
| 2.9 | 端口面板 | 「🖧 端口」显示当前运行端口 + PID + 进程名 | ☐ |
| 2.10 | dsh 版本下拉 | 每环境可指定 dsh 实例（默认 / 具体版本） | ☐ |

## 三、插件市场

| # | 验收项 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| 3.1 | 浏览 | 中文描述优先、分类/★星标/下载量展示 | ☐ |
| 3.2 | 搜索 | 关键字过滤（300ms 防抖） | ☐ |
| 3.3 | 排序 | 下拉「默认 / 🔥 热门（下载量）/ 🆕 最新」 | ☐ |
| 3.4 | 单个安装 | 「安装」→ 已安装徽标出现；可更新/卸载 | ☐ |
| 3.5 | 批量安装 | 勾选多个 →「批量安装（N）」→ 队列面板显示进度（⏳→🔄→✅/❌+失败原因） | ☐ |
| 3.6 | 失败提示 | 失败时明确标出**哪个包**失败及原因 | ☐ |

## 四、插件分配

| # | 验收项 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| 4.1 | 环境树 | 环境面板可展开/收起，显示分配数与可添加数 | ☐ |
| 4.2 | 同环境排序 | 拖拽卡片排序（乐观更新 + 写回 cordis.patch.yml） | ☐ |
| 4.3 | 跨环境移动 | 拖到其它环境面板 → 分配关系迁移，旧/新环境 patch 同步 | ☐ |
| 4.4 | **可用插件拖拽** | 「可用插件」区（已安装依赖 + bundles）chip **拖到任意环境即新建分配** | ☐ |
| 4.5 | 点选添加 | 「从已安装添加」下拉点选（不输入 ID） | ☐ |
| 4.6 | 启停/顺序 | 卡片启用/禁用、上移/下移、移除；右键菜单一致 | ☐ |
| 4.7 | 写回保护 | 分配自动写回；**patch 含复杂结构（嵌套/!!js）时拒绝写回并提示**，不破坏用户配置 | ☐ |

## 五、内核管理

| # | 验收项 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| 5.1 | 统一内核卡 | 启用开关、插件增删/排序/启停、「应用到所有环境」「还原」 | ☐ |
| 5.2 | **按环境覆盖** | 每环境下拉：跟随全局 / 强制注入 / 跳过注入（保存即时生效） | ☐ |
| 5.3 | 内核实例 | 新建（模板+Profile+端口）、启动/停止、日志、删除 | ☐ |

## 六、DSH 环境 / 设置

| # | 验收项 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| 6.1 | DSH 环境页 | base/并列/外部三类环境、版本显示、设为默认、添加并列环境（版本下拉） | ☐ |
| 6.2 | 主题/语言 | 设置页切换深色/浅色/跟随系统；中/英切换 | ☐ |
| 6.3 | 文件位置 | DSH 根目录可改（重启生效提示）、其它只读展示 | ☐ |
| 6.4 | 市场设置 | 市场启用开关 + URL | ☐ |
| 6.5 | 数据备份 | 导出 JSON / 导入恢复 | ☐ |

## 七、安全与工程（本轮加固）

| # | 验收项 | 通过标准 | 结果 |
| --- | --- | --- | --- |
| 7.1 | CORS 收紧 | 同源请求**无** `Access-Control-Allow-Origin: *`；`X-Content-Type-Options: nosniff` 等安全头存在 | ☐ |
| 7.2 | patch 备份 | 写回 patch 前自动备份到 `data/patches-backup/` | ☐ |
| 7.3 | 版本控制 | git 仓库已初始化（11 个提交），tag `v0.2.2` 存在 | ☐ |
| 7.4 | 自动化验证 | `pnpm test`（20 单测）+ `pnpm test:smoke`（5 组冒烟）全绿 | ☐ |

---

## 验收结论

- [ ] 全部通过，可发布
- [ ] 存在问题（见下）

### 问题记录
| 编号 | 描述 | 复现步骤 | 影响 |
| --- | --- | --- | --- |
|  |  |  |  |
