# DSH 内部原生可视化插件窗口设计方案 (设计方案.md)

> **版本**：v0.5.0 (In-DSH Native Visual Workbench Architecture)  
> **核心目标**：彻底打破“外部管理工具”与“DSH 内部对话”的割裂感，将 godsh 的环境治理、热插拔矩阵、工作流与任务中心直接无缝嵌入 DSH 官方 Web 界面内部。

---

## 一、 核心痛点与架构愿景

### 1. 为什么纯对话（形态 B）不够方便？
- **自然语言交互冗长**：简单的“查看已安装插件”或“开启某个插件”，打字对话需要 5~10 秒，而图形界面点击只需 0.1 秒；
- **缺乏直观态势感知**：无法一眼看清哪些插件正在生效、当前环境依赖健康度、内存/Token 消耗；
- **切屏割裂**：开发者在 DSH 对话写代码时，还要切到 5173 端口的外部管理面板。

### 2. 方案愿景：原生一体化嵌入
在 DSH 官方 Web 界面（左侧主导航栏 / 顶部悬浮栏 / 对话消息流）直接开辟 **godsh 原生可视化抽屉与面板**，实现：
**“代码对话在中央，环境管理在侧翼，流水线任务在浮窗，一键操作零切屏”**。

---

## 二、 原生挂载技术架构 (DSH Slot Architecture)

DSH 客户端基于 `@deepseek-ai/dsh-client-ui-slots` 插槽机制构建，我们将 godsh 客户端组件分层注册进 DSH 标准插槽：

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                         DSH 官方 Web 界面 (Native Window)                   │
├───────────────┬──────────────────────────────────────────────┬──────────────┤
│  左侧主侧边栏  │              中央主工作区                    │  右侧辅助栏  │
│ (Slot: item)  │                                              │(Slot: panel) │
├───────────────┼──────────────────────────────────────────────┼──────────────┤
│ 💬 会话列表    │                                              │ ⚡ godsh     │
│ 📁 代码工作区  │               AI 对话与代码编辑器             │   原生控制台 │
│ 🧩 插件生态   │                                              │ ──────────── │
│               │                                              │ • 环境切换器 │
│ ⚡ godsh      │  [AI 消息卡片: 工作流建议]                    │ • 插件热开关 │
│   (点击展开)   │  ┌────────────────────────────────────────┐  │ • 1秒快照备份│
│               │  │ 一键安装开发套件? [确认执行] [预览差异] │  │ • 一键工作流 │
│               │  └────────────────────────────────────────┘  │              │
├───────────────┴──────────────────────────────────────────────┴──────────────┤
│ 底部浮动层 (Slot: global.overlay) ➔ [⚡ 全局任务中心: 编译进度 85% 流式回显]│
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1. 插槽注册清单
| DSH 插槽名称 | 挂载组件 | 交互形态 | 核心功能 |
| :--- | :--- | :--- | :--- |
| `workbench.sidebar.item` | `GodshSidebarButton` | 左侧专属图标按钮 | 点击展开侧边抽屉工作台 |
| `workbench.panel` | `GodshControlPanel` | 右侧/弹窗主控制面板 | 环境切换、插件热插拔开关矩阵、一键工作流 |
| `global.overlay` | `GodshFloatingTaskHud` | 底部右下角常驻 HUD | 任务执行流式回显、状态胶囊、错误一键诊断 |
| `conversation.card` | `GodshActionCard` | 嵌入 AI 对话气泡 | 自然语言建议与富交互按钮联动 |

---

## 三、 核心功能组件设计

### 1. 组件 A：In-DSH 插件热插拔矩阵 (Hot Toggle Matrix)
- **视觉设计**：轻量级 Switch 开关列表，展示每个插件的图标、分类标签、加载状态。
- **底层联动**：切换 Switch 时，通过 `ctx.godsh.patch` 写入 `cordis.patch.yml`，触发 Cordis 内部 HMR，**界面 0.5 秒内变为绿色“已激活”状态，完全无需刷新网页**。

### 2. 组件 B：一键流水线抽屉 (One-Click Workflow Drawer)
- **视觉设计**：卡片式展示「AI Agent 栈」、「全栈开发套件」、「极简纯净环境」。
- **底层联动**：点击直接调用内部 `WorkflowRunner`，并在底部 HUD 实时打出进度条与日志。

### 3. 组件 C：秒级安全快照与灾难恢复条 (Snapshot Bar)
- **视觉设计**：位于面板顶部的安全控制条，提供 `[📸 拍摄快照]` 与 `[历史快照下拉框]`。
- **底层联动**：任何重大安装前自动打快照，一键还原 `package.json` 与 `cordis.patch.yml`。

---

## 四、 通信机制与状态同构 (State Sync)

采用 **Cordis EventBus + RPC 双向通道**：
1. **Host ➔ Client 推送**：当 DSH 内部发生插件变动、工作流推进或快照生成时，通过 `ctx.emit("godsh/state-update")` 瞬间推送到前端 UI；
2. **Client ➔ Host 调用**：UI 点击直接调用 `ctx.remotes.godsh.*`，无任何跨域或网络延迟。

---

## 五、 实施计划路线图

- **第一阶段 (P0)**：在 `@godsh/dsh-plugin/src/client` 中完善 `workbench.sidebar.item` 抽屉视图；
- **第二阶段 (P1)**：实现富交互 `GodshActionCard`，让 AI 在对话中直接输出可点击的操作卡片；
- **第三阶段 (P2)**：打通底部常驻 HUD 与全局任务中心双向联动。
