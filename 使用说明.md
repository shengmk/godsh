# godsh 完整使用说明书 (User Guide)

> **版本**：v0.4.1+ (原生插件化与可视化工作台双模版)  
> **核心定位**：DSH (DeepSeek Harness) 的「Anaconda Navigator」——可视化环境治理、零重启热插拔与 AI Agent 动态工具箱。

---

## 🌟 核心概念：两大界面形态，各司其职

很多初学者容易混淆 **godsh 可视化工作台** 与 **DSH 官方对话界面**。godsh 提供了两种完全不同但深度联动的体验形态：

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                             godsh 双重使用形态                               │
├──────────────────────────────────────┬──────────────────────────────────────┤
│  形态 A：godsh 可视化管理工作台       │  形态 B：DSH 官方智能体 Web 对话端    │
│  (GUI Workbench)                     │  (DSH Native Agent Chat)             │
├──────────────────────────────────────┼──────────────────────────────────────┤
│ • 打开方式：pnpm serve + pnpm dev:web │ • 打开方式：dsh --profile test-profile│
│ • 访问地址：http://localhost:5173     │ • 访问地址：http://127.0.0.1:3080/3081│
│ • 界面风格：类似 Anaconda Navigator  │ • 界面风格：DeepSeek 官方 AI 对话环境│
│ • 主要功能：                         │ • 主要功能：                         │
│   - 可视化查看/创建 Profile 环境     │   - 在对话框直接用自然语言给 AI 派活 │
│   - 10 秒一键执行自动化装机工作流    │   - AI 自动调用 godsh 插件工具管理环境│
│   - 插件市场浏览、星标、一键安装     │   - 自动沉淀长期思维记忆             │
│   - 右下角常驻全局任务中心流式日志   │   - 零重启实时感知插件热插拔         │
└──────────────────────────────────────┴──────────────────────────────────────┘
```

---

## 🖥️ 形态 A：godsh 可视化管理工作台（操作指南）

如果你想要直观的图形化界面来管理环境、搜索插件和执行装机：

### 1. 启动工作台
在项目根目录打开 PowerShell 终端：
```powershell
# 终端 1：启动后台 API 服务
pnpm serve

# 终端 2：启动前端 Web 界面
pnpm dev:web
```
启动后，浏览器打开 **`http://localhost:5173`**。

### 2. 界面功能与操作详解

#### ① 【环境管理页 (Profiles)】
- **查看与切换**：左侧/顶部列出所有环境（如 `dev-workspace`、`test-profile`、`web`）。
- **执行极速工作流 (Workflows)**：
  1. 点击顶部 **`⚡ 工作流 / 规则`** 按钮；
  2. 选择内置方案（如 **开发者套件**、**AI 对话套件**、**极简纯净套件**）；
  3. 点击 **`执行工作流`**；
  4. 右下角任务中心会立即弹出，**10~25 秒内自动完成环境创建、插件检索与单批次极速安装**！
- **多版本快照热备份**：
  - 点击环境卡片上的 **`📸 创建快照`**，备份当前全量配置；
  - 遇到配置破坏时，点击 **`⏪ 一键回滚`** 秒级还原。
- **环境包导出与导入**：点击 **`📤 导出环境包`** 即可生成 `.profile.json`，实现跨机器秒级环境复现。

#### ② 【插件市场页 (Marketplace)】
- 实时拉取 DSH 官方插件生态；
- 支持按热度、下载量、星标排序；
- 点击 **`安装到环境`** 即可指定注入到某个 Profile。

#### ③ 【全局常驻任务中心 (Task Center)】
- 位于界面右下角的悬浮徽标与黑色抽屉；
- 具备 `KeepAlive` 常驻特性，切页时后台任务不会中断，终端日志实时滚动流式回显。

---

## 🤖 形态 B：DSH 原生智能体对话（操作指南）

如果你在 DSH 官方 Web 界面中与 DeepSeek 模型进行日常编程与对话：

### 1. 启动 DSH 原生环境
在终端运行：
```powershell
# 指定端口启动测试环境（避免与已运行的 3080 端口冲突）
$env:PORT=3081; dsh --profile test-profile
```
浏览器会自动打开 **`http://127.0.0.1:3081`** 进入 DeepSeek 官方对话界面。

### 2. 在对话中直接使用 4 大 AI 动态工具
由于我们在 `test-profile` 中挂载了 `@godsh/dsh-plugin`，AI 对话模型已具备了「环境管理员」身份，你可以直接在输入框对它说话：

| 你在对话框输入的自然语言指令 | AI 自动触发的底层 Tool | 实际执行效果 |
| :--- | :--- | :--- |
| **“帮我看一下当前系统里有哪些环境和插件”** | `godsh_list_profiles` | AI 自动查询并结构化列出所有 Profile、bundles 与依赖清单 |
| **“帮我在 test-profile 执行开发套件工作流”** | `godsh_workflow_execute` | AI 自动触发自动化流水线，完成装机并反馈结果 |
| **“帮我把插件 dsh-update-checker 禁用掉”** | `godsh_toggle_plugin_hot` | AI 零重启修改 `cordis.patch.yml`，Cordis 1 秒内瞬间热卸载 |
| **“在做大改动前帮我为 test-profile 做个备份”** | `godsh_snapshot_backup` | AI 自动生成全量快照存档并返回快照 ID |

---

## ⚡ 常见问题与排错手册

### Q1: 运行 `dsh --profile test-profile` 提示端口被占用 (`EADDRINUSE 3080`)？
- **解答**：系统后台有旧的 DSH 实例在运行。
  - **方法一（推荐）**：换端口启动：`$env:PORT=3081; dsh --profile test-profile`；
  - **方法二**：关闭旧进程：在 PowerShell 执行 `Stop-Process -Id 15488 -Force`。

### Q2: 工作流安装很慢或卡在假包？
- **解答**：godsh v0.4.1+ 已彻底解决该问题：
  - 自动将虚拟分类映射为真实市场插件；
  - 开启单次批量 `pnpm add`；
  - 无代理时自动接入 `npmmirror` 镜像源加速。

---

## 🎯 总结建议

- **想用图形界面配置、批量安装、管理环境？** ➔ 使用 **形态 A (http://localhost:5173)**
- **想在对话写代码时让 AI 帮你热调整环境？** ➔ 使用 **形态 B (http://127.0.0.1:3081)**
