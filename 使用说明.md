# dsh Launcher 使用说明

> 面向最终用户与开发者的完整使用手册。

## 目录

1. [项目简介与架构](#1-项目简介与架构)
2. [功能详解](#2-功能详解)
3. [CLI 命令参考](#3-cli-命令参考)
4. [HTTP API 参考](#4-http-api-参考)
5. [配置文件说明](#5-配置文件说明)
6. [桌面应用（Tauri）构建与运行](#6-桌面应用tauri构建与运行)
7. [常见问题与故障排查](#7-常见问题与故障排查)
8. [开发指南](#8-开发指南)

---

## 1. 项目简介与架构

dsh Launcher 是 **DeepSeek Harness（dsh）** 的图形化环境配置启动器，类比 Anaconda Navigator：

| Anaconda | DSH | dsh Launcher |
| --- | --- | --- |
| Anaconda Navigator | dsh web / CLI | 本启动器 |
| conda env | DSH Profile | 环境管理 |
| pip/conda 包 | dsh plugin / bundle | 插件分配与安装 |
| conda channel | dshmarket | 插件市场 |
| Python | DeepSeek Harness 核心 | dsh 本体（底层调用） |

### 架构

```text
┌─────────────────────────────────────────┐
│  UI 层                                  │
│  ├─ Web 界面（apps/shell-web, Vite+React）│
│  └─ 桌面壳（Tauri 2, apps/launcher/src-tauri）│
└──────────────┬──────────────────────────┘
               │ HTTP (端口 4780)
┌──────────────▼──────────────────────────┐
│  Node API 后端（apps/launcher/server.ts）│
│  环境/插件/市场/分配/内核                 │
└──────────────┬──────────────────────────┘
               │ 调用 dsh CLI / 读写配置
┌──────────────▼──────────────────────────┐
│  DSH Profile（$DSH_HOME/profiles）      │
│  package.json · cordis.patch.yml        │
└─────────────────────────────────────────┘
```

- **同一个 Web 内核**：所有 client 插件统一由 dsh 的 `dsh-client-web` 内核加载，不重复启动浏览器。
- **桌面应用自包含**：后端 `server.mjs` 与内核模板打进资源；运行时数据存 `%APPDATA%\dsh-launcher\data`。

---

## 2. 功能详解

### 2.1 环境管理

- 自动扫描 `$DSH_HOME/profiles/`，展示每个环境的 bundles、依赖、patch 状态。
- **启动**：拉起 `dsh --profile <name> --port <port> --no-open`，等待端口就绪后标记「运行中」。
  > 注意：dsh 的真实启动命令没有 `web` 字样 —— `dsh web` 是 `--profile web` 的硬编码别名，不接受 `--profile`。
- **停止**：按 pid 文件结束进程树。
- **日志**：实时轮询（每 2s）日志文件，可暂停/继续，自动滚动到底部。
- **右键菜单**：启动/停止、打开 Web UI、查看日志、复制端口/地址。

### 2.2 插件市场

- 数据源：`https://awesome-dsh-plugin.com/plugins.json`（可在 `data/config.json` 修改）。
- 搜索（客户端过滤）、安装到指定环境（`dsh plugin --profile <name> add <pkg>`）。
- 来源校验：registry/local 默认允许，git 来源需白名单（`packages/security`）。

### 2.3 插件分配

- 把插件分配给环境（记录在 `data/allocations.json`），控制启用/禁用/顺序。
- **应用（写回 patch）**：把分配关系落为环境的 `cordis.patch.yml`（只重写管理的插件，保留用户自定义条目）。
- **拖拽排序**：调整启用的先后顺序；右键菜单支持上移/下移/移除。

### 2.4 内核管理

- 内核模板：`kernels/templates/*/template.json`（如 `web-default`）。
- 内核实例：新建（选模板/环境/端口）、启动、停止、删除，状态持久化在 `data/kernels.json`。

---

## 3. CLI 命令参考

```powershell
# 源码模式运行 CLI
cd dsh-launcher
pnpm launcher <command>

# 或使用打包后的单文件后端
node apps/launcher/dist/server.mjs <command>
```

| 命令 | 说明 |
| --- | --- |
| `detect` | 检测 node/pnpm/dsh 与 DSH_HOME |
| `profiles` | 扫描 `$DSH_HOME/profiles` |
| `profile create <name>` / `profile remove <name>` | 新建/删除环境 |
| `plugins` | 扫描本地 `plugins/` 插件 |
| `market` | 拉取插件市场索引 |
| `plugin <add|remove|update|ls> <pkg> --profile <name>` | 安装/卸载/更新/列出插件 |
| `start <profile> [--port <n>]` | 前台启动 dsh web（Ctrl+C 停止） |
| `stop <profile> [--port <n>]` | 停止 dsh web |
| `status [--port <n>]` | 查询运行状态 |
| `kernels` / `kernel <create|start|stop|remove>` | 内核模板与实例 |
| `allocate <profile> <pluginId> [--disable]` | 分配插件 |
| `allocations <profile>` / `apply <profile>` / `sync` / `unallocate <id>` | 分配关系管理与落地 |
| `serve [--port <n>]` | 启动 HTTP API（默认 4780） |
| `help` | 帮助 |

---

## 4. HTTP API 参考

API 服务由 `pnpm serve`（或桌面应用内置后端）提供，监听 `127.0.0.1:4780`。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/health` | 环境检测摘要 |
| GET | `/api/profiles` | 环境列表（含运行状态） |
| POST | `/api/profiles/:name/start` | 启动环境 Web UI |
| POST | `/api/profiles/:name/stop` | 停止环境 |
| GET | `/api/profiles/:name/log` | 日志尾部 |
| GET | `/api/profiles/:name/status` | 端口就绪状态 |
| GET | `/api/plugins` | 本地插件 |
| GET | `/api/market?q=` | 插件市场（可搜索） |
| POST | `/api/profiles/:name/plugins` `{action,pkg}` | 安装/卸载/更新 |
| GET/POST | `/api/allocations` | 分配关系 |
| PATCH/DELETE | `/api/allocations/:id` | 启停/删除分配 |
| POST | `/api/allocations/apply` | 写回环境 patch |
| POST | `/api/allocations/reorder` | 调整顺序 |
| GET/POST | `/api/kernels` | 内核模板 + 实例 |
| POST | `/api/kernels/:id/start` / `/stop` | 内核启停 |
| DELETE | `/api/kernels/:id` | 删除内核实例 |

---

## 5. 配置文件说明

### `data/config.json`（Launcher 全局配置）

```jsonc
{
  "launcher": { "name": "dsh Launcher", "version": "0.1.0" },
  "dsh": { "home": "C:\\Users\\<你>\\.dsh", "bin": "dsh", "profilesDir": "profiles" },
  "webKernel": { "defaultTemplateId": "web-default", "defaultPort": 3080 },
  "pluginMarket": { "enabled": true, "indexUrl": "https://awesome-dsh-plugin.com/plugins.json" },
  "dataDir": "./data"
}
```

### 其它数据文件

| 文件 | 说明 |
| --- | --- |
| `data/allocations.json` | 插件分配关系 |
| `data/kernels.json` | 内核实例 |
| `data/runtime/service-pid-<port>.txt` | 运行中服务的 pid |
| `data/logs/` | 各环境启动日志 |
| `%APPDATA%\dsh-launcher\data\` | **桌面应用**的运行时数据目录 |

### 环境（Profile）配置

- `$DSH_HOME/profiles/<name>/package.json`：`dsh.profile.bundles` 决定启动时加载的插件层。
- `$DSH_HOME/profiles/<name>/cordis.patch.yml`：启用/禁用/配置插件（Launcher 写回目标）。

---

## 6. 桌面应用（Tauri）构建与运行

### 运行已打包版本

```powershell
# 自包含，无需环境变量
& "$env:USERPROFILE\dshlauncher\target\debug\dsh-launcher.exe"   # debug
# 或 release：
& "$env:USERPROFILE\dshlauncher\apps\launcher\src-tauri\target\release\dsh-launcher.exe"
```

### 从源码构建

```powershell
pwsh -File scripts/build-tauri.ps1
# 步骤：pnpm build:server（打包后端）→ build:tauri（构建前端）→ 填充 resources → cargo build
```

### 打包发布产物（安装器 + 便携 ZIP）

```powershell
# 1) 先构建 release exe（见上节；或用 tauri build 生成 target/release 产物）
# 2) 打包：
pwsh -File scripts/make-release.ps1
# 产物（release/ 目录）：
#   dsh-launcher-0.1.0-x64-setup.exe   NSIS 安装器
#   dsh-launcher-0.1.0-x64.zip         便携版
#   SHA256SUMS.txt                     校验和
```

> **WebView2Loader.dll**：GNU 工具链下它是 WebView 初始化必需的动态依赖，
> 打包时被放在 exe 旁（安装器 / ZIP 均已包含）。**不要单独拷走 exe**，否则会报「找不到 WebView2Loader.dll」。

### 环境变量（开发用）

| 变量 | 说明 |
| --- | --- |
| `DSH_LAUNCHER_DATA_DIR` | 运行时数据目录（默认 `%APPDATA%\dsh-launcher\data`） |
| `DSH_LAUNCHER_TEMPLATES_DIR` | 内核模板目录（默认资源内 `templates/`） |
| `DSH_LAUNCHER_SERVER` | 后端 `server.mjs` 路径（开发时指定，打包后从资源解析） |

---

## 7. 常见问题与故障排查

**Q1：启动环境后一直「启动中…」**
- 检查端口是否被占用（`netstat -ano | findstr <port>`）。
- 打开该环境的日志面板，看是否有 `cordis.patch.yml` 格式错误等提示。

**Q2：桌面应用打不开或白屏 / 报「找不到 WebView2Loader.dll」**
- 确认 `WebView2Loader.dll` 与 `dsh-launcher.exe` 在同一目录（安装器 / ZIP 已自带；不要单独拷走 exe）。
- 确认已装 WebView2（Windows 11 自带；Win10 需安装 Evergreen Runtime）。
- 确认系统有 `node`（桌面应用内置后端依赖 Node 子进程）。

**Q3：插件安装失败**
- 检查来源校验：git 来源需在 `packages/security` 白名单中。
- 底层是 `dsh plugin --profile <name> add <pkg>`（转发给 pnpm），可先手动执行看报错。

**Q4：打包桌面应用报「路径含空格」**
- 这是 GNU 工具链 + windres 的已知限制：把 `src-tauri/` 与前端 dist 复制到无空格路径构建（`build-tauri.ps1` 已处理）。

---

## 8. 开发指南

### 项目结构

```text
dsh-launcher/
├─ apps/
│  ├─ launcher/            # Node CLI + HTTP API + src-tauri(Tauri 壳)
│  └─ shell-web/           # Vite + React + TS 前端
├─ packages/
│  ├─ core/                # 环境检测、进程管理、配置存储、事件总线
│  ├─ profile-manager/     # Profile 扫描/编辑/patch
│  ├─ plugin-registry/     # bundle/client 插件识别
│  ├─ marketplace/         # dsh plugin 封装 + 市场索引
│  ├─ kernel-manager/      # 内核模板与实例
│  ├─ allocation/          # 插件分配
│  └─ security/            # 来源校验、构建授权
├─ kernels/templates/      # 内核模板
├─ plugins/                # 本地插件示例
├─ data/                   # 运行时数据
├─ scripts/                # dev/build/bump-version/pack 脚本
└─ docs/                   # 设计文档与迭代日志
```

### 常用开发命令

```powershell
pnpm install              # 安装依赖
pnpm typecheck            # 后端类型检查
pnpm build:web            # 构建前端（Web 模式，/api 同源）
pnpm --filter @dsh-launcher/shell-web build:tauri   # 构建前端（Tauri 模式，指向 4780）
pnpm build:server         # 打包后端为单文件 apps/launcher/dist/server.mjs
pnpm launcher <command>   # 运行 CLI
pnpm serve                # 启动 API + 托管前端（Web 模式）
pnpm dev:web              # Vite 开发服务器（/api 代理到 4780）
pwsh -File scripts/build-tauri.ps1   # 构建 Tauri 桌面壳
pwsh -File scripts/bump-version.ps1 -Version 0.2.0   # 统一升级版本号
```

### 版本号管理

所有版本号（package.json / Cargo.toml / tauri.conf.json / config.json）统一通过：

```powershell
pwsh -File scripts/bump-version.ps1 -Version 0.2.0
```

### 设计文档

- `docs/dsh-study.md`：DSH 框架学习笔记
- `docs/workflow.md`：业务流程
- `docs/folder-config.md`：目录与配置约定
- `docs/gui-design.md`：GUI 风格设计
- `docs/iteration-log.md`：迭代日志
