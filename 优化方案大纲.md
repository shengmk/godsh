# godsh 优化方案大纲（下一迭代输入提示词）

> 本文件作为**下一个开发会话的完整输入提示词**。请按此大纲逐步实现，并在实现前先读
> `项目文件总结.md`、`docs/iteration-log.md`、`docs/dsh-study.md` 了解现状与 DSH 真实机制。
>
> 用户已确认满意的部分：环境页、插件市场页（中文）。以下全部为**待优化项**。

---

## 0. 总体目标

把 godsh 从「可用的管理工具」升级为「像 Anaconda Navigator 一样顺手、美观、可配置的一体化环境启动器」：

1. **统一内核**：每个 Profile 都能用统一 Web 内核启动（即使该 Profile 的 bundles 里没有 `dsh-web-app`）；内核可配置、可添加用户偏好的插件。
2. **可视化插件分配**：Profile 可展开成树，插件直接拖拽移动/启停，不再靠输入 ID。
3. **dsh 版本管理**：在 Launcher 内管理/切换多个 dsh 版本（环境配置发生在 dsh 之外）。
4. **一体化界面 + 设置页**：语言、文件/数据存放位置、主题（深/浅/跟随系统）。
5. **界面优化**：侧边栏去掉数字快捷键、改为页面功能说明；整体 UI 更美观。

---

## 1. 统一内核（高优先级）

### 1.1 现状与问题
- 现状：`环境页`启动 Profile 执行 `dsh --profile <name> --port <port> --no-open`。
- 问题：**bundles 里没有 `dsh-web-app` 的 Profile（如 `open-design`、`experiment`）无法提供 Web UI**，启动后无界面。
- 用户诉求：每个 Profile 都能运行「统一内核」；内核可修改（用户可往里加自己喜欢的插件）。

### 1.2 目标
- 任何 Profile 都能启动出 Web UI（统一 `dsh-client-web` 内核）。
- Launcher 提供一个「统一内核配置」：一组用户偏好的插件（bundle/client），注入到每个 Profile 的启动层。
- 内核可修改：增删插件、调整顺序、切换模板。

### 1.3 方案
- **方案 A（推荐）— 启动时注入 patch 层**：
  - 在启动命令中加 `--patch <统一内核.patch.yml>`，把「统一内核插件层」叠加到任意 Profile 上。
  - 统一内核层内容：`dsh-web-app`（若 Profile 缺）+ 用户偏好的 client/bundle 插件。
  - 涉及：`packages/core/process-manager.ts`（启动命令加 --patch）、新增 `packages/kernel-manager` 的「统一内核配置」模型、`data/unified-kernel.json`。
- **方案 B — 拷贝/修改 Profile**：为无 web-app 的 Profile 自动补 bundles（改 package.json）。风险：污染用户 Profile，不推荐。
- **UI**：环境页启动按钮对所有 Profile 可用；新增「统一内核」配置入口（设置页或内核管理页）。
- **验证**：`open-design`（bundles 只有 dsh-base）点击启动后能打开 Web UI；统一内核加一个 client 插件后所有 Profile 的 UI 都出现该插件。

---

## 2. 插件分配可视化（高优先级）

### 2.1 现状与问题
- 现状：`插件分配页`需要**手填插件 ID**（输入框），再点「分配」。
- 用户诉求：**Profile 可展开**，下面显示已装/已分配插件，**直接拖拽**调整顺序、移动、启停，无需输入 ID。

### 2.2 目标
- 左侧：Profile 树（可展开）。
- 展开后：该 Profile 的插件列表（已分配 + 已安装）。
- 支持：拖拽排序、拖到其它 Profile、切换启用/禁用、从市场/已安装列表拖入、右键菜单（上移/下移/移除/查看）。
- 后端：现有 `allocations.json` + `reorder` API 已支持顺序；需补充「按已安装依赖自动生成可分配列表」。

### 2.3 方案
- **数据**：Profile 的「可分配插件」= 该 Profile `dependencies`（已安装）+ 市场/本地插件（可选）。
- **后端**：`GET /api/profiles/:name/plugins` 已返回 dependencies；新增 `GET /api/allocations/:profile/available`（合并已安装 + 可分配）。
- **前端**：AllocationsPage 重做：
  - 折叠面板：Profile 头（名称/状态/插件数）可展开；
  - 展开后插件卡片可拖拽（复用现有 drag 逻辑）+ 拖拽到其它 Profile（需要后端 `moveAllocation(profile)` 或直接 reorder + 改 profile 字段）；
  - 增加「从已安装添加」按钮（无需输入 ID，从下拉/列表点选）。
- **后端补充**：`Allocation` 增加「来源」（installed/assigned）与「已安装但未分配」的自动提示。
- **验证**：不输入任何 ID，仅通过点选/拖拽完成分配与排序。

---

## 3. dsh 版本管理（中优先级）

### 3.1 现状与问题
- 现状：Launcher 只调用 PATH 里的 `dsh`（单一版本）。
- 用户诉求：在 Launcher 内**配置/切换不同版本 dsh**，即「环境配置在 dsh 之外」。

### 3.2 目标
- 检测已安装的多个 dsh（PATH、npm 全局、指定目录）。
- 每个 Profile / 内核实例可选择使用哪个 dsh 版本。
- 可新增「dsh 安装目录」配置（设置页）。

### 3.3 方案
- **后端**：`packages/core/env-detect.ts` 扩展：
  - `findDshInstances()`：扫描 PATH、`npm root -g`、用户配置的目录，收集 `dsh --version`。
  - 启动命令使用指定 dsh 的**绝对路径**（而非 PATH 里的 `dsh`）。
- **数据**：`data/config.json` 增加 `dsh.instances`（名称→路径）+ `dsh.activeVersion`；Profile/内核可覆盖。
- **UI**：设置页「DSH 运行时」区：列出已检测版本、设为默认；环境页/内核页可指定版本。
- **验证**：安装两个 dsh 版本后能在 UI 切换，启动命令用对应版本。

---

## 4. 设置页（中优先级）

### 4.1 目标
新增「设置」页（侧边栏入口），配置：

| 设置项 | 说明 |
| --- | --- |
| 语言 | `zh-CN` / `en`（前端 i18n 框架） |
| 文件位置 | DSH 根目录、Launcher 数据目录、日志目录、内核模板目录（可修改 + 恢复默认） |
| 主题 | 浅色 / 深色 / 跟随系统 |
| DSH 版本 | 见第 3 节 |
| 市场 | 市场 URL、启用/禁用 |
| 构建授权 | pnpm allowBuilds 白名单（复用 security 包） |

### 4.2 方案
- **i18n**：引入 `react-i18next` 或自建轻量语言包（`src/i18n/zh-CN.ts`、`en.ts`）；先只抽离 UI 文案（插件内容来自市场，保持原文/zh 优先）。
- **主题**：`styles.css` 用 CSS 变量（已部分使用 `:root` 变量）；新增 `data-theme="dark"` 覆盖变量；`App.tsx` 管理主题状态并持久化到 `localStorage`。
- **文件位置**：`config.json` 已支持部分路径；设置页写入后需要 `paths.ts` 支持运行时读取（当前是模块级常量——需改为可配置或重启生效 + 提示）。
- **UI**：新增 `SettingsPage.tsx` + 侧边栏入口；玻璃拟态风格延续。
- **验证**：切深色主题立即生效；改语言后导航/按钮文案变化；改数据目录后重启生效。

---

## 5. 一体化界面 + 侧边栏优化（中优先级）

### 5.1 侧边栏
- **去掉数字 `1234` 角标**（用户明确要求）。
- 同一位置改为显示**该页面的功能说明**（一行小字），便于使用。
- 保留图标/点状标识，可选保留 `Ctrl+1..4` 快捷键（去掉数字角标即可）。

### 5.2 一体化界面
- 首页仪表盘：环境总数/运行中/插件数/内核数 卡片 + 快捷操作。
- 导航更清晰：环境 / 插件市场 / 插件分配 / 内核管理 / 设置。

---

## 6. UI 美化（中优先级，贯穿全部页面）

### 6.1 方向
- 延续玻璃拟态（`#2563EB→#38BDF8` 渐变、半透明白卡、模糊、大圆角），但：
  - 卡片 hover 动效、入场动画（fade/slide）；
  - 状态徽标更精致（脉冲、渐变边框）；
  - 空状态插画/文案；
  - 加载骨架屏（Skeleton）；
  - 深色主题完整适配（当前只做了浅色）。
- 图标：引入轻量图标库（如 `lucide-react`）替换 emoji/文字。
- 统一间距/字号/圆角令牌（CSS 变量集中管理）。

### 6.2 组件化
- 抽离 `Card`、`Badge`、`Button`、`Modal`、`Tooltip`、`Skeleton`、`Empty`、`Switch`、`Tabs` 等通用组件到 `src/components/`，各页复用。

---

## 7. 我的补充改进点（功能）

> 除用户反馈外，建议一并考虑：

| 项 | 说明 |
| --- | --- |
| 启动进度可视化 | 环境页启动时显示阶段（spawn → 端口就绪 → 完成），当前只有「启动中」徽标 |
| 内核实例日志查看 | 内核页实例也能看日志（复用 profile 日志接口） |
| 插件安装进度 | 市场安装走 pnpm，可能耗时；加进度提示/安装中状态 |
| 环境批量操作 | 全选/批量启停 |
| 数据备份 | 一键导出/导入 `data/`（config/kernels/allocations） |
| 进程端口管理 | 显示所有端口占用与释放冲突提示 |
| 通知/托盘 | Tauri 托盘 + 系统通知（GUI 问卷遗留项） |
| 错误诊断 | 启动失败时展示 `dsh.log` 尾部并给出可读修复建议 |
| 版本升级流程 | `bump-version.ps1` 已具备，可加 `pnpm` 侧自动同步 |

---

## 8. 我的补充改进点（界面）

| 项 | 说明 |
| --- | --- |
| 响应式 | 窗口缩放/窄屏时布局自适应 |
| 键盘无障碍 | 焦点样式、可 Tab 导航 |
| 右键菜单统一 | 各页右键菜单风格一致（已做，可细化） |
| 顶栏 | 全局搜索框（快速启动环境/插件）、当前 dsh 版本显示 |
| 设置预览 | 主题切换即时预览 |

---

## 9. 实施顺序建议

1. **P0（先把体验补上）**
   - 侧边栏去数字 + 功能说明（前端小改）
   - 插件分配可视化（第 2 节）
2. **P0（核心能力）**
   - 统一内核（第 1 节）
3. **P1**
   - 设置页：主题深色 → 语言 → 文件位置（第 4 节）
   - dsh 版本管理（第 3 节）
4. **P1**
   - 一体化仪表盘 + UI 美化（第 5、6 节）
5. **P2**
   - 第 7、8 节的补充改进（按需）

> 建议每完成一个 P0/P1 模块，更新 `docs/iteration-log.md`，并跑 `pnpm typecheck` + `pnpm build:web`（必要时 `make-release.ps1` 重新打包验证桌面端）。

---

## 10. 风险与注意

- **统一内核的 patch 叠加**：需验证 `--patch` 多层叠加语义（DSH 按顺序叠加：profile bundles → profile patch → home patch → CLI --patch）。参考 `docs/dsh-study.md`。
- **不污染用户 Profile**：统一内核用 `--patch` 注入，不直接改用户 `package.json`/`cordis.patch.yml`。
- **GNU 工具链**：桌面打包必须 `tauri build`（内嵌前端）；路径不能含空格；`WebView2Loader.dll` 在 exe 旁。见 `make-release.ps1`。
- **数据迁移**：改数据目录后，旧数据（kernels/allocations）需提示或自动迁移。
