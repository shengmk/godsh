

---

## 版本发布规范（重要）

- **每次发布新版本时，保留所有旧版本 Release，不删除**。
- 每个版本发布时：
  1. git tag vX.Y.Z <commit> 打版本标签
  2. 创建 Release 时指定该 tag
  3. 上传安装包 / 便携版 / 源码包 / SHA256SUMS
  4. **不要删除任何历史 Release**（用户可能依赖旧版本）
- 旧版本若缺少安装包产物，可随时从对应 git tag 重新构建。